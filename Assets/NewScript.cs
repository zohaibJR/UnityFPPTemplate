using UnityEngine;

public class NewScript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("GitHub Pushed");
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
